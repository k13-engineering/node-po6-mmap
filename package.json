{
  "type": "module",
  "main": "lib/index.ts",
  "scripts": {
    "build": "rm -rf dist/ && deno-node-build --root . --out dist/ --entry lib/index.ts",
    "test": "c8 --reporter lcov --reporter html --reporter text --all --src lib/ --exclude lib/snippets/ --exclude test/ mocha --expose-gc test/**/*.ts",
    "lint": "eslint .",
    "update-deps": "npm-check-updates -u"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@k13engineering/releasetool": "^0.0.5",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.5",
    "c8": "^10.1.3",
    "deno-node": "^0.0.12",
    "mocha": "^11.7.5",
    "npm-check-updates": "^19.3.2",
    "typescript-eslint": "^8.19.0"
  },
  "dependencies": {
    "buffer2address": "^0.0.8",
    "syscall-napi": "^0.1.5"
  }
}
